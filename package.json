{
  "name": "starmade-blockeditor",
  "version": "1.0.0",
  "description": "Visual block editor for StarMade — 3D preview with texture atlas mapping, powered by StarMade-Decoder",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "start": "npm run start --workspace=server",
    "test": "npm run test --workspace=server && npm run test --workspace=client",
    "coverage": "npm run coverage --workspace=server && npm run coverage --workspace=client"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.5",
    "concurrently": "^8.2.2",
    "vitest": "^4.1.5"
  }
}
